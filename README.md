# React Base Template (API + Validaciones)

Base reutilizable con:

- React + TypeScript + Vite
- Tailwind CSS
- React Router
- Zustand (tema persistente + contenido API global)
- React Hook Form + Zod (validaciones en frontend)

## Scripts

```bash
npm install
npm run dev
npm run build
npm run lint
```
